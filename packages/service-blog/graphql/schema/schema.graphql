type Blog @key(fields: "id") {
  id: ID!
  title: String!
  content: String!
  createdAt: String!
  updatedAt: String!
}

type Query {
  blog(id: ID!): Blog!
  blogs: [Blog!]!
  blogsByUserId(userId: ID!): [Blog!]!
}

type Mutation {
  deleteBlog(id: ID!): String!
}
